# --- Go (Golang) ---

*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out

# Go Workspace (本機開發用，通常不推上去以免影響協作者)
go.work
go.work.sum

# 如果你有用 vendor 模式 (目前你用 module 模式，通常不需要這行，但加了保險)
vendor/

# --- Node.js / Frontend (React + Vite) ---
# 依賴套件 (絕對不能推！)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Build 產出的靜態檔案
dist/
build/

# 本地測試覆蓋率報告
coverage/

# Vite 相關暫存
.vite/

# --- Tilt & Kubernetes ---
# Tilt 擴充套件下載區 (因為你有用 ext://restart_process)
tilt_modules/

# 本地的 Tilt 狀態紀錄
.tilt-dev/
.tilt-*.json

# 敏感的 Kubeconfig (通常不在專案內，但防呆一下)
*.kubeconfig

# --- Environment Variables (資安關鍵！) ---
# 包含 API Key 或密碼的檔案絕對不能推
.env
.env.local
.env.*.local

# --- IDE / Editor ---
# VS Code 設定 (如果你不想共享你的個人設定)
.vscode/
# JetBrains (GoLand/WebStorm)
.idea/
*.iml

# --- OS System Files ---
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db
desktop.ini

# --- Terraform (基礎設施) ---
# 本地下載的 Plugin 與模組 (類似 node_modules)
.terraform/

# 狀態檔 (⚠️ 絕對不能推！包含敏感資訊與當前狀態)
*.tfstate
*.tfstate.*

# 崩潰日誌
crash.log
crash.*.log

# 變數檔 (通常包含密碼或個人設定)
*.tfvars
*.tfvars.json

# 忽略覆寫設定 (本地除錯用)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# CLI 設定檔
.terraformrc
terraform.rc

# --- Kubernetes / Helm ---
# Helm 下載的依賴 (如果以後有用 helm dependency update)
charts/

# 確保 Secret 絕對不會被推上去 (你原本有寫，這裡再次強調)
deploy/k8s/secret.yaml
secrets.yaml
*.secret.yaml